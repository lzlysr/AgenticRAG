工具分布对不上应该是温度为0.2和0.7的问题！温度越低，em和f1越高，但工具分布的semantic_search和graph_search为0。温度改成和初版一样的0.7，em和f1大幅降低，但工具分布对得上。

将max_len由300提升为500，所有指标全部大幅增长，效果提升巨大。
目前只做了 Qwen3-4B-sft-zh 的 500 提升。

问题：

SFT之前
两种模型表现出的是两种相反的失败模式：
Qwen3-4B：过早停止，检索不足
Qwen3.5-4B：持续调用工具，检索过度

这个问题跟温度没关系。

单独增强系统提示词也没啥用。

说明：
Qwen3：工具格式输出不够稳定 → 被程序提前终止
Qwen3.5：工具格式输出很稳定 → 一直被程序允许继续

改进：
尝试在最后一轮加提示词强制输出 answer，但还是没用。sft模式还是在执行工具调用。

排查发现 Qwen3.5 喜欢一次吐出两个 <tool_call>，有没有可能是重复检索的根源？

加了提示词限制之后，确实没有一次吐出多个 <tool_call> 了，但最后给出的答案也还是偏长、偏解释化。

因为其实目前只约束了最后一轮强制轮的答案要精炼，没约束正常结束轮的答案。

约束正常结束轮的答案要简短之后，好像还是输出很多解释内容。

无法解决Qwen3.5-4B：持续调用工具，检索过度的问题。没办法了，只能退回到 Qwen3。